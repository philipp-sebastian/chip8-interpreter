#include "../include/common.h"

int main(void)
{
    Chip8_t* pChip8 = initializeComponents();

    if (pChip8 == NULL)
    {
        printf("Closing Program");
        return -1;
    }

    while (1)
    {
        //imlementation
        break;
    }
}
